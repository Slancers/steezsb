// Same image as OpenGraph — Twitter card uses the same canonical 1200×630
// render. Re-export keeps a single source of truth.
export { default, runtime, alt, size, contentType } from "./opengraph-image";
