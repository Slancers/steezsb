// Document types that should exist as exactly one document.
// Used by the Studio structure and action filters to prevent duplicates.
export const singletonTypes = new Set<string>(["siteSettings"]);
